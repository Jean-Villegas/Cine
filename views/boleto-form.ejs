<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Comprar Boleto - CineApp</title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
    <style>
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            margin: 0;
            padding: 0;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
        }
        .container {
            max-width: 800px;
            margin: 0 auto;
            padding: 20px;
        }
        .header {
            background: rgba(255, 255, 255, 0.95);
            padding: 30px;
            border-radius: 15px;
            box-shadow: 0 10px 30px rgba(0,0,0,0.2);
            text-align: center;
            margin-bottom: 30px;
        }
        .header h1 {
            color: #2c3e50;
            margin: 0;
            font-size: 2.5em;
        }
        .form-container {
            background: rgba(255, 255, 255, 0.95);
            border-radius: 15px;
            padding: 30px;
            box-shadow: 0 10px 30px rgba(0,0,0,0.2);
            margin-bottom: 30px;
        }
        .form-group {
            margin-bottom: 20px;
        }
        .form-group label {
            display: block;
            margin-bottom: 8px;
            color: #2c3e50;
            font-weight: bold;
        }
        .form-group input,
        .form-group select {
            width: 100%;
            padding: 12px;
            border: 2px solid #ecf0f1;
            border-radius: 8px;
            font-size: 1em;
            transition: border-color 0.3s ease;
            box-sizing: border-box;
        }
        .form-group input:focus,
        .form-group select:focus {
            outline: none;
            border-color: #3498db;
        }
        .form-row {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 20px;
        }
        .btn {
            background: #3498db;
            color: white;
            padding: 12px 24px;
            border: none;
            border-radius: 8px;
            cursor: pointer;
            text-decoration: none;
            display: inline-block;
            margin: 5px;
            font-size: 1em;
            transition: background 0.3s ease;
        }
        .btn:hover {
            background: #2980b9;
        }
        .btn-success {
            background: #27ae60;
        }
        .btn-success:hover {
            background: #229954;
        }
        .btn-secondary {
            background: #95a5a6;
        }
        .btn-secondary:hover {
            background: #7f8c8d;
        }
        .form-actions {
            text-align: center;
            margin-top: 30px;
        }
        .loading {
            text-align: center;
            color: white;
            font-size: 1.2em;
            padding: 40px;
        }
        .success {
            background: #27ae60;
            color: white;
            padding: 15px;
            border-radius: 8px;
            text-align: center;
            margin: 20px 0;
        }
        .error {
            background: #e74c3c;
            color: white;
            padding: 15px;
            border-radius: 8px;
            text-align: center;
            margin: 20px 0;
        }
        .back-btn {
            position: absolute;
            top: 20px;
            left: 20px;
            background: rgba(255, 255, 255, 0.9);
            color: #2c3e50;
            padding: 10px 20px;
            border-radius: 8px;
            text-decoration: none;
            font-weight: bold;
        }
        .required {
            color: #e74c3c;
        }
        .info-box {
            background: #e8f5e8;
            border: 1px solid #4caf50;
            border-radius: 8px;
            padding: 15px;
            margin: 20px 0;
            color: #2e7d32;
        }
        .funcion-info {
            background: #f8f9fa;
            border: 1px solid #dee2e6;
            border-radius: 8px;
            padding: 15px;
            margin: 15px 0;
            display: none;
        }
        .funcion-info h4 {
            color: #2c3e50;
            margin: 0 0 10px 0;
        }
        .funcion-detail {
            color: #7f8c8d;
            margin: 5px 0;
        }
        .funcion-detail strong {
            color: #2c3e50;
        }
    </style>
</head>
<body>
    <a href="/boletos" class="back-btn">← Volver a Boletos</a>
    
    <div class="container">
        <div class="header">
            <h1>🎫 Comprar Boleto</h1>
            <p>Selecciona la función y completa los datos para comprar tu boleto</p>
        </div>

        <div class="form-container">
            <div class="info-box">
                <strong>ℹ️ Información:</strong> 
                Primero selecciona una función disponible. El precio se mostrará automáticamente 
                y se generará un código QR único para tu boleto.
            </div>

            <form id="boletoForm">
                <div class="form-group">
                    <label for="funcionId">Función <span class="required">*</span></label>
                    <select id="funcionId" name="funcionId" required>
                        <option value="">Selecciona una función</option>
                    </select>
                </div>

                <div id="funcionInfo" class="funcion-info">
                    <h4>📋 Información de la Función</h4>
                    <div id="funcionDetails"></div>
                </div>

                <div class="form-row">
                    <div class="form-group">
                        <label for="usuarioId">Usuario <span class="required">*</span></label>
                        <select id="usuarioId" name="usuarioId" required>
                            <option value="">Selecciona un usuario</option>
                        </select>
                    </div>
                    <div class="form-group">
                        <label for="numeroAsiento">Número de Asiento <span class="required">*</span></label>
                        <input type="text" id="numeroAsiento" name="numeroAsiento" placeholder="Ej: A15, B25, C10" required>
                    </div>
                </div>

                <div class="form-group">
                    <label for="metodoPago">Método de Pago</label>
                    <select id="metodoPago" name="metodoPago">
                        <option value="tarjeta">Tarjeta de Crédito/Débito</option>
                        <option value="efectivo">Efectivo</option>
                        <option value="transferencia">Transferencia Bancaria</option>
                        <option value="paypal">PayPal</option>
                    </select>
                </div>

                <div class="form-actions">
                    <button type="submit" class="btn btn-success">🎫 Comprar Boleto</button>
                    <button type="button" class="btn btn-secondary" onclick="resetForm()">🔄 Limpiar Formulario</button>
                    <a href="/boletos" class="btn btn-secondary">❌ Cancelar</a>
                </div>
            </form>
        </div>

        <div id="messageContainer"></div>
    </div>

    <script>
        const API_BASE = '/api/boletos';
        const FUNCIONES_API = '/api/funciones';
    const USUARIOS_API = '/api/usuarios';

        // Cargar funciones y usuarios al cargar la página
        window.onload = async function() {
            await loadFunciones();
            await loadUsuarios();
        };

        async function loadFunciones() {
            try {
                const response = await fetch(FUNCIONES_API);
                const data = await response.json();
                
                if (data.success) {
                    const select = document.getElementById('funcionId');
                    // Filtrar solo funciones activas con asientos disponibles
                    const funcionesDisponibles = data.data.filter(funcion => 
                        funcion.estado === 'activa' && funcion.asientosDisponibles > 0
                    );
                    
                    funcionesDisponibles.forEach(funcion => {
                        const option = document.createElement('option');
                        option.value = funcion.id;
                        option.textContent = `${funcion.pelicula ? funcion.pelicula.titulo : 'Película no disponible'} - ${funcion.sala ? funcion.sala.numero : 'Sala no disponible'} - ${funcion.fecha} ${funcion.horaInicio}`;
                        option.dataset.precio = funcion.precio;
                        option.dataset.pelicula = funcion.pelicula ? funcion.pelicula.titulo : 'No disponible';
                        option.dataset.sala = funcion.sala ? funcion.sala.numero : 'No disponible';
                        option.dataset.fecha = funcion.fecha;
                        option.dataset.hora = funcion.horaInicio;
                        option.dataset.asientos = funcion.asientosDisponibles;
                        select.appendChild(option);
                    });
                }
            } catch (error) {
                console.error('Error al cargar funciones:', error);
            }
        }

        async function loadUsuarios() {
            try {
                const response = await fetch(USUARIOS_API + '/activos');
                const data = await response.json();
                
                if (data.success) {
                    const select = document.getElementById('usuarioId');
                    data.data.forEach(usuario => {
                        const option = document.createElement('option');
                        option.value = usuario.id;
                        option.textContent = `${usuario.nombre} ${usuario.apellido} (${usuario.email})`;
                        select.appendChild(option);
                    });
                }
            } catch (error) {
                console.error('Error al cargar usuarios:', error);
            }
        }

        // Mostrar información de la función seleccionada
        document.getElementById('funcionId').addEventListener('change', function() {
            const selectedOption = this.options[this.selectedIndex];
            const funcionInfo = document.getElementById('funcionInfo');
            const funcionDetails = document.getElementById('funcionDetails');
            
            if (selectedOption.dataset.precio) {
                funcionDetails.innerHTML = `
                    <div class="funcion-detail"><strong>Película:</strong> ${selectedOption.dataset.pelicula}</div>
                    <div class="funcion-detail"><strong>Sala:</strong> ${selectedOption.dataset.sala}</div>
                    <div class="funcion-detail"><strong>Fecha:</strong> ${selectedOption.dataset.fecha}</div>
                    <div class="funcion-detail"><strong>Hora:</strong> ${selectedOption.dataset.hora}</div>
                    <div class="funcion-detail"><strong>Asientos disponibles:</strong> ${selectedOption.dataset.asientos}</div>
                    <div class="funcion-detail"><strong>Precio:</strong> $${selectedOption.dataset.precio}</div>
                `;
                funcionInfo.style.display = 'block';
            } else {
                funcionInfo.style.display = 'none';
            }
        });

        document.getElementById('boletoForm').addEventListener('submit', async function(e) {
            e.preventDefault();
            
            const formData = new FormData(e.target);
            const boletoData = {
                funcionId: parseInt(formData.get('funcionId')),
                usuarioId: parseInt(formData.get('usuarioId')),
                numeroAsiento: formData.get('numeroAsiento'),
                metodoPago: formData.get('metodoPago')
            };

            try {
                showLoading();
                
                const response = await fetch(API_BASE, {
                    method: 'POST',
                    headers: {
                        'Content-Type': 'application/json'
                    },
                    body: JSON.stringify(boletoData)
                });

                const data = await response.json();
                
                if (data.success) {
                    showSuccess('¡Boleto comprado exitosamente! 🎫<br>Código QR: <strong>' + data.data.codigoQR + '</strong>');
                    resetForm();
                    setTimeout(() => {
                        window.location.href = '/boletos';
                    }, 3000);
                } else {
                    showError('Error al comprar el boleto: ' + data.message);
                }
            } catch (error) {
                showError('Error de conexión: ' + error.message);
            }
        });

        function resetForm() {
            document.getElementById('boletoForm').reset();
            document.getElementById('metodoPago').value = 'tarjeta';
            document.getElementById('funcionInfo').style.display = 'none';
        }

        function showLoading() {
            const container = document.getElementById('messageContainer');
            container.innerHTML = '<div class="loading">🔄 Comprando boleto...</div>';
        }

        function showSuccess(message) {
            const container = document.getElementById('messageContainer');
            container.innerHTML = `<div class="success">${message}</div>`;
        }

        function showError(message) {
            const container = document.getElementById('messageContainer');
            container.innerHTML = `<div class="error">${message}</div>`;
        }
    </script>
</body>
</html>

