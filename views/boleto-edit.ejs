<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Editar Boleto - CineApp</title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
    <style>
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            margin: 0;
            padding: 0;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
        }
        .container {
            max-width: 800px;
            margin: 0 auto;
            padding: 20px;
        }
        .header {
            background: rgba(255, 255, 255, 0.95);
            padding: 30px;
            border-radius: 15px;
            box-shadow: 0 10px 30px rgba(0,0,0,0.2);
            text-align: center;
            margin-bottom: 30px;
        }
        .header h1 {
            color: #2c3e50;
            margin: 0;
            font-size: 2.5em;
        }
        .form-container {
            background: rgba(255, 255, 255, 0.95);
            border-radius: 15px;
            padding: 30px;
            box-shadow: 0 10px 30px rgba(0,0,0,0.2);
            margin-bottom: 30px;
        }
        .form-group {
            margin-bottom: 20px;
        }
        .form-group label {
            display: block;
            margin-bottom: 8px;
            color: #2c3e50;
            font-weight: bold;
        }
        .form-group input,
        .form-group select {
            width: 100%;
            padding: 12px;
            border: 2px solid #ecf0f1;
            border-radius: 8px;
            font-size: 1em;
            transition: border-color 0.3s ease;
            box-sizing: border-box;
        }
        .form-group input:focus,
        .form-group select:focus {
            outline: none;
            border-color: #3498db;
        }
        .form-row {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 20px;
        }
        .btn {
            background: #3498db;
            color: white;
            padding: 12px 24px;
            border: none;
            border-radius: 8px;
            cursor: pointer;
            text-decoration: none;
            display: inline-block;
            margin: 5px;
            font-size: 1em;
            transition: background 0.3s ease;
        }
        .btn:hover {
            background: #2980b9;
        }
        .btn-success {
            background: #27ae60;
        }
        .btn-success:hover {
            background: #229954;
        }
        .btn-secondary {
            background: #95a5a6;
        }
        .btn-secondary:hover {
            background: #7f8c8d;
        }
        .form-actions {
            text-align: center;
            margin-top: 30px;
        }
        .loading {
            text-align: center;
            color: white;
            font-size: 1.2em;
            padding: 40px;
        }
        .success {
            background: #27ae60;
            color: white;
            padding: 15px;
            border-radius: 8px;
            text-align: center;
            margin: 20px 0;
        }
        .error {
            background: #e74c3c;
            color: white;
            padding: 15px;
            border-radius: 8px;
            text-align: center;
            margin: 20px 0;
        }
        .back-btn {
            position: absolute;
            top: 20px;
            left: 20px;
            background: rgba(255, 255, 255, 0.9);
            color: #2c3e50;
            padding: 10px 20px;
            border-radius: 8px;
            text-decoration: none;
            font-weight: bold;
        }
        .required {
            color: #e74c3c;
        }
        .info-box {
            background: #fff3cd;
            border: 1px solid #ffeaa7;
            border-radius: 8px;
            padding: 15px;
            margin: 20px 0;
            color: #856404;
        }
    </style>
</head>
<body>
    <a href="/boletos" class="back-btn">← Volver a Boletos</a>
    
    <div class="container">
        <div class="header">
            <h1>✏️ Editar Boleto</h1>
            <p>Modifica la información del boleto</p>
        </div>

        <div class="form-container">
            <div class="info-box">
                <strong>⚠️ Advertencia:</strong> Ten cuidado al modificar boletos ya vendidos. 
                Cambios en asientos o funciones pueden causar conflictos.
            </div>

            <form id="boletoForm">
                <div class="form-row">
                    <div class="form-group">
                        <label for="funcionId">Función <span class="required">*</span></label>
                        <select id="funcionId" name="funcionId" required>
                            <option value="">Selecciona una función</option>
                        </select>
                    </div>
                    <div class="form-group">
                        <label for="usuarioId">Usuario <span class="required">*</span></label>
                        <select id="usuarioId" name="usuarioId" required>
                            <option value="">Selecciona un usuario</option>
                        </select>
                    </div>
                </div>

                <div class="form-row">
                    <div class="form-group">
                        <label for="numeroAsiento">Número de Asiento <span class="required">*</span></label>
                        <input type="text" id="numeroAsiento" name="numeroAsiento" required>
                    </div>
                    <div class="form-group">
                        <label for="precio">Precio ($)</label>
                        <input type="number" id="precio" name="precio" min="0" step="0.01">
                    </div>
                </div>

                <div class="form-row">
                    <div class="form-group">
                        <label for="fechaCompra">Fecha de Compra</label>
                        <input type="date" id="fechaCompra" name="fechaCompra">
                    </div>
                    <div class="form-group">
                        <label for="horaCompra">Hora de Compra</label>
                        <input type="time" id="horaCompra" name="horaCompra">
                    </div>
                </div>

                <div class="form-row">
                    <div class="form-group">
                        <label for="estado">Estado</label>
                        <select id="estado" name="estado">
                            <option value="confirmado">Confirmado</option>
                            <option value="usado">Usado</option>
                            <option value="cancelado">Cancelado</option>
                        </select>
                    </div>
                    <div class="form-group">
                        <label for="metodoPago">Método de Pago</label>
                        <select id="metodoPago" name="metodoPago">
                            <option value="tarjeta">Tarjeta</option>
                            <option value="efectivo">Efectivo</option>
                            <option value="transferencia">Transferencia</option>
                            <option value="puntos">Puntos</option>
                        </select>
                    </div>
                </div>

                <div class="form-group">
                    <label for="codigoQR">Código QR</label>
                    <input type="text" id="codigoQR" name="codigoQR" readonly>
                </div>

                <div class="form-actions">
                    <button type="submit" class="btn btn-success">✅ Actualizar Boleto</button>
                    <a href="/boletos" class="btn btn-secondary">❌ Cancelar</a>
                </div>
            </form>
        </div>

        <div id="messageContainer"></div>
    </div>

    <script>
        const API_BASE = '/api/boletos';
        const FUNCIONES_API = '/api/funciones';
        const USUARIOS_API = '/api/users';
        let boletoId = null;

        // Cargar datos del boleto al cargar la página
        window.onload = async function() {
            // Obtener ID de la URL
            const urlParams = new URLSearchParams(window.location.search);
            boletoId = urlParams.get('id');
            
            if (boletoId) {
                await loadFunciones();
                await loadUsuarios();
                await loadBoleto(boletoId);
            } else {
                showError('ID de boleto no proporcionado');
            }
        };

        async function loadFunciones() {
            try {
                const response = await fetch(FUNCIONES_API);
                const data = await response.json();
                
                if (data.success) {
                    const select = document.getElementById('funcionId');
                    data.data.forEach(funcion => {
                        const option = document.createElement('option');
                        option.value = funcion.id;
                        option.textContent = `${funcion.pelicula?.titulo || 'Película'} - ${funcion.fecha} ${funcion.horaInicio}`;
                        option.dataset.precio = funcion.precio;
                        select.appendChild(option);
                    });
                }
            } catch (error) {
                console.error('Error al cargar funciones:', error);
            }
        }

        async function loadUsuarios() {
            try {
                const response = await fetch(USUARIOS_API);
                const data = await response.json();
                
                if (data.success) {
                    const select = document.getElementById('usuarioId');
                    data.data.forEach(usuario => {
                        const option = document.createElement('option');
                        option.value = usuario.id;
                        option.textContent = `${usuario.nombre} ${usuario.apellido || ''} (${usuario.email})`;
                        select.appendChild(option);
                    });
                }
            } catch (error) {
                console.error('Error al cargar usuarios:', error);
            }
        }

        async function loadBoleto(id) {
            try {
                const response = await fetch(`${API_BASE}/${id}`);
                const data = await response.json();
                
                if (data.success) {
                    const boleto = data.data;
                    
                    // Llenar el formulario con los datos existentes
                    document.getElementById('funcionId').value = boleto.funcionId || '';
                    document.getElementById('usuarioId').value = boleto.usuarioId || '';
                    document.getElementById('numeroAsiento').value = boleto.numeroAsiento || '';
                    document.getElementById('precio').value = boleto.precio || '';
                    document.getElementById('fechaCompra').value = boleto.fechaCompra || '';
                    document.getElementById('horaCompra').value = boleto.horaCompra || '';
                    document.getElementById('estado').value = boleto.estado || 'confirmado';
                    document.getElementById('metodoPago').value = boleto.metodoPago || 'tarjeta';
                    document.getElementById('codigoQR').value = boleto.codigoQR || '';
                } else {
                    showError('Error al cargar el boleto: ' + data.message);
                }
            } catch (error) {
                showError('Error de conexión: ' + error.message);
            }
        }

        // Auto-calcular precio cuando cambie la función
        document.getElementById('funcionId').addEventListener('change', function() {
            const selectedOption = this.options[this.selectedIndex];
            if (selectedOption.dataset.precio) {
                document.getElementById('precio').value = selectedOption.dataset.precio;
            }
        });

        document.getElementById('boletoForm').addEventListener('submit', async function(e) {
            e.preventDefault();
            
            const formData = new FormData(e.target);
            const boletoData = {
                funcionId: parseInt(formData.get('funcionId')),
                usuarioId: parseInt(formData.get('usuarioId')),
                numeroAsiento: formData.get('numeroAsiento'),
                precio: parseFloat(formData.get('precio')) || 0,
                fechaCompra: formData.get('fechaCompra'),
                horaCompra: formData.get('horaCompra'),
                estado: formData.get('estado'),
                metodoPago: formData.get('metodoPago'),
                codigoQR: formData.get('codigoQR')
            };

            try {
                showLoading();
                
                const response = await fetch(`${API_BASE}/${boletoId}`, {
                    method: 'PUT',
                    headers: {
                        'Content-Type': 'application/json'
                    },
                    body: JSON.stringify(boletoData)
                });

                const data = await response.json();
                
                if (data.success) {
                    showSuccess('¡Boleto actualizado exitosamente!');
                    setTimeout(() => {
                        window.location.href = '/boletos';
                    }, 2000);
                } else {
                    showError('Error al actualizar el boleto: ' + data.message);
                }
            } catch (error) {
                showError('Error de conexión: ' + error.message);
            }
        });

        function showLoading() {
            const container = document.getElementById('messageContainer');
            container.innerHTML = '<div class="loading">🔄 Actualizando boleto...</div>';
        }

        function showSuccess(message) {
            const container = document.getElementById('messageContainer');
            container.innerHTML = `<div class="success">${message}</div>`;
        }

        function showError(message) {
            const container = document.getElementById('messageContainer');
            container.innerHTML = `<div class="error">${message}</div>`;
        }
    </script>
</body>
</html>
