<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Detalle de Boleto - CineApp</title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
    <style>
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            margin: 0;
            padding: 0;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
        }
        .container {
            max-width: 800px;
            margin: 0 auto;
            padding: 20px;
        }
        .header {
            background: rgba(255, 255, 255, 0.95);
            padding: 30px;
            border-radius: 15px;
            box-shadow: 0 10px 30px rgba(0,0,0,0.2);
            text-align: center;
            margin-bottom: 30px;
        }
        .header h1 {
            color: #2c3e50;
            margin: 0;
            font-size: 2.5em;
        }
        .detail-container {
            background: rgba(255, 255, 255, 0.95);
            border-radius: 15px;
            padding: 30px;
            box-shadow: 0 10px 30px rgba(0,0,0,0.2);
            margin-bottom: 30px;
        }
        .detail-section {
            margin-bottom: 25px;
            padding: 20px;
            background: #f8f9fa;
            border-radius: 10px;
            border-left: 4px solid #3498db;
        }
        .detail-section h3 {
            color: #2c3e50;
            margin: 0 0 15px 0;
            font-size: 1.3em;
        }
        .detail-item {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 8px 0;
            border-bottom: 1px solid #ecf0f1;
        }
        .detail-item:last-child {
            border-bottom: none;
        }
        .detail-label {
            font-weight: bold;
            color: #2c3e50;
        }
        .detail-value {
            color: #7f8c8d;
        }
        .status-badge {
            display: inline-block;
            padding: 5px 12px;
            border-radius: 20px;
            font-size: 0.8em;
            font-weight: bold;
            text-transform: uppercase;
        }
        .status-confirmado {
            background: #d5f4e6;
            color: #27ae60;
        }
        .status-usado {
            background: #e3f2fd;
            color: #1976d2;
        }
        .status-cancelado {
            background: #fadbd8;
            color: #e74c3c;
        }
        .qr-display {
            background: white;
            padding: 30px;
            border-radius: 15px;
            text-align: center;
            margin: 20px 0;
            border: 3px solid #3498db;
        }
        .qr-code {
            background: #f8f9fa;
            padding: 20px;
            border-radius: 10px;
            font-family: 'Courier New', monospace;
            font-size: 1.2em;
            color: #2c3e50;
            margin: 15px 0;
            border: 2px dashed #3498db;
            word-break: break-all;
        }
        .boleto-header {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            padding: 20px;
            border-radius: 15px;
            text-align: center;
            margin: 20px 0;
        }
        .boleto-number {
            font-size: 2em;
            font-weight: bold;
            margin: 10px 0;
        }
        .actions {
            text-align: center;
            margin-top: 30px;
        }
        .btn {
            background: #3498db;
            color: white;
            padding: 12px 24px;
            border: none;
            border-radius: 8px;
            cursor: pointer;
            text-decoration: none;
            display: inline-block;
            margin: 5px;
            font-size: 1em;
            transition: background 0.3s ease;
        }
        .btn:hover {
            background: #2980b9;
        }
        .btn-success {
            background: #27ae60;
        }
        .btn-success:hover {
            background: #229954;
        }
        .btn-danger {
            background: #e74c3c;
        }
        .btn-danger:hover {
            background: #c0392b;
        }
        .btn-secondary {
            background: #95a5a6;
        }
        .btn-secondary:hover {
            background: #7f8c8d;
        }
        .loading {
            text-align: center;
            color: white;
            font-size: 1.2em;
            padding: 40px;
        }
        .error {
            background: #e74c3c;
            color: white;
            padding: 15px;
            border-radius: 8px;
            text-align: center;
            margin: 20px 0;
        }
        .back-btn {
            position: absolute;
            top: 20px;
            left: 20px;
            background: rgba(255, 255, 255, 0.9);
            color: #2c3e50;
            padding: 10px 20px;
            border-radius: 8px;
            text-decoration: none;
            font-weight: bold;
        }
        .print-btn {
            background: #f39c12;
            color: white;
            padding: 12px 24px;
            border: none;
            border-radius: 8px;
            cursor: pointer;
            text-decoration: none;
            display: inline-block;
            margin: 5px;
            font-size: 1em;
            transition: background 0.3s ease;
        }
        .print-btn:hover {
            background: #e67e22;
        }
    </style>
</head>
<body>
    <a href="/boletos" class="back-btn">← Volver a Boletos</a>
    
    <div class="container">
        <div class="header">
            <h1>🎫 Detalle de Boleto</h1>
            <p>Información completa del boleto seleccionado</p>
        </div>

        <div id="boletoContainer">
            <div class="loading">
                <p>🔄 Cargando información del boleto...</p>
            </div>
        </div>
    </div>

    <script>
        const API_BASE = '/api/boletos';

        // Obtener ID de la URL
        const boletoId = window.location.pathname.split('/').pop();

        async function loadBoletoDetail() {
            try {
                const response = await fetch(`${API_BASE}/${boletoId}`);
                const data = await response.json();
                
                if (data.success) {
                    displayBoletoDetail(data.data);
                } else {
                    showError('Error al cargar el boleto: ' + data.message);
                }
            } catch (error) {
                showError('Error de conexión: ' + error.message);
            }
        }

        function displayBoletoDetail(boleto) {
            const container = document.getElementById('boletoContainer');
            
            const statusClass = `status-${boleto.estado}`;
            
            container.innerHTML = `
                <div class="boleto-header">
                    <h2>🎬 CineApp</h2>
                    <div class="boleto-number">Boleto #${boleto.id}</div>
                    <p>¡Disfruta tu película!</p>
                </div>

                <div class="detail-container">
                    <div class="detail-section">
                        <h3>🎬 Información de la Función</h3>
                        <div class="detail-item">
                            <span class="detail-label">Película:</span>
                            <span class="detail-value">${boleto.funcion && boleto.funcion.pelicula ? boleto.funcion.pelicula.titulo : 'No disponible'}</span>
                        </div>
                        <div class="detail-item">
                            <span class="detail-label">Sala:</span>
                            <span class="detail-value">${boleto.funcion && boleto.funcion.sala ? boleto.funcion.sala.numero : 'No disponible'}</span>
                        </div>
                        <div class="detail-item">
                            <span class="detail-label">Fecha:</span>
                            <span class="detail-value">${boleto.funcion ? boleto.funcion.fecha : 'No disponible'}</span>
                        </div>
                        <div class="detail-item">
                            <span class="detail-label">Horario:</span>
                            <span class="detail-value">${boleto.funcion ? `${boleto.funcion.horaInicio} - ${boleto.funcion.horaFin}` : 'No disponible'}</span>
                        </div>
                        <div class="detail-item">
                            <span class="detail-label">Asiento:</span>
                            <span class="detail-value">${boleto.numeroAsiento}</span>
                        </div>
                    </div>

                    <div class="detail-section">
                        <h3>👤 Información del Usuario</h3>
                        <div class="detail-item">
                            <span class="detail-label">Nombre:</span>
                            <span class="detail-value">${boleto.usuario ? `${boleto.usuario.nombre} ${boleto.usuario.apellido}` : 'No disponible'}</span>
                        </div>
                        <div class="detail-item">
                            <span class="detail-label">Email:</span>
                            <span class="detail-value">${boleto.usuario ? boleto.usuario.email : 'No disponible'}</span>
                        </div>
                        <div class="detail-item">
                            <span class="detail-label">Tipo de Usuario:</span>
                            <span class="detail-value">${boleto.usuario ? boleto.usuario.tipoUsuario : 'No disponible'}</span>
                        </div>
                    </div>

                    <div class="detail-section">
                        <h3>💳 Información de Pago</h3>
                        <div class="detail-item">
                            <span class="detail-label">Precio:</span>
                            <span class="detail-value">$${boleto.precio}</span>
                        </div>
                        <div class="detail-item">
                            <span class="detail-label">Método de Pago:</span>
                            <span class="detail-value">${boleto.metodoPago}</span>
                        </div>
                        <div class="detail-item">
                            <span class="detail-label">Fecha de Compra:</span>
                            <span class="detail-value">${boleto.fechaCompra}</span>
                        </div>
                        <div class="detail-item">
                            <span class="detail-label">Hora de Compra:</span>
                            <span class="detail-value">${boleto.horaCompra}</span>
                        </div>
                        <div class="detail-item">
                            <span class="detail-label">Estado:</span>
                            <span class="detail-value">
                                <span class="status-badge ${statusClass}">${boleto.estado}</span>
                            </span>
                        </div>
                    </div>

                    <div class="qr-display">
                        <h3>📱 Código QR</h3>
                        <div class="qr-code">${boleto.codigoQR}</div>
                        <p><small>Presenta este código en la entrada del cine</small></p>
                    </div>

                    <div class="actions">
                        <button class="btn" onclick="printBoleto()">🖨️ Imprimir Boleto</button>
                        <button class="btn btn-success" onclick="usarBoleto(${boleto.id})" ${boleto.estado === 'usado' ? 'disabled' : ''}>
                            ✅ ${boleto.estado === 'usado' ? 'Ya Usado' : 'Marcar como Usado'}
                        </button>
                        <button class="btn btn-danger" onclick="cancelarBoleto(${boleto.id})" ${boleto.estado === 'cancelado' ? 'disabled' : ''}>
                            ❌ ${boleto.estado === 'cancelado' ? 'Ya Cancelado' : 'Cancelar Boleto'}
                        </button>
                        <a href="/boletos" class="btn btn-secondary">📋 Ver Todos los Boletos</a>
                    </div>
                </div>
            `;
        }

        function showError(message) {
            const container = document.getElementById('boletoContainer');
            container.innerHTML = `<div class="error">${message}</div>`;
        }

        async function usarBoleto(id) {
            if (confirm('¿Marcar este boleto como usado?')) {
                try {
                    const response = await fetch(`${API_BASE}/${id}/usar`, {
                        method: 'PUT'
                    });
                    const data = await response.json();
                    
                    if (data.success) {
                        alert('Boleto marcado como usado exitosamente');
                        loadBoletoDetail(); // Recargar la página
                    } else {
                        alert('Error al usar el boleto: ' + data.message);
                    }
                } catch (error) {
                    alert('Error al usar el boleto: ' + error.message);
                }
            }
        }

        async function cancelarBoleto(id) {
            if (confirm('¿Estás seguro de que quieres cancelar este boleto?')) {
                try {
                    const response = await fetch(`${API_BASE}/${id}`, {
                        method: 'DELETE'
                    });
                    const data = await response.json();
                    
                    if (data.success) {
                        alert('Boleto cancelado exitosamente');
                        window.location.href = '/boletos';
                    } else {
                        alert('Error al cancelar el boleto: ' + data.message);
                    }
                } catch (error) {
                    alert('Error al cancelar el boleto: ' + error.message);
                }
            }
        }

        function printBoleto() {
            window.print();
        }

        // Cargar detalles al cargar la página
        window.onload = function() {
            loadBoletoDetail();
        };
    </script>
</body>
</html>

