<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Funciones - CineApp</title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
    <style>
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            margin: 0;
            padding: 0;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
        }
        .container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 20px;
        }
        .header {
            background: rgba(255, 255, 255, 0.95);
            padding: 30px;
            border-radius: 15px;
            box-shadow: 0 10px 30px rgba(0,0,0,0.2);
            text-align: center;
            margin-bottom: 30px;
        }
        .header h1 {
            color: #2c3e50;
            margin: 0;
            font-size: 2.5em;
        }
        .actions {
            background: rgba(255, 255, 255, 0.95);
            padding: 20px;
            border-radius: 15px;
            box-shadow: 0 8px 25px rgba(0,0,0,0.15);
            margin-bottom: 30px;
            text-align: center;
        }
        .btn {
            background: #3498db;
            color: white;
            padding: 12px 24px;
            border: none;
            border-radius: 8px;
            cursor: pointer;
            text-decoration: none;
            display: inline-block;
            margin: 5px;
            font-size: 1em;
            transition: background 0.3s ease;
        }
        .btn:hover {
            background: #2980b9;
        }
        .btn-success {
            background: #27ae60;
        }
        .btn-success:hover {
            background: #229954;
        }
        .btn-danger {
            background: #e74c3c;
        }
        .btn-danger:hover {
            background: #c0392b;
        }
        .funciones-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
            gap: 20px;
            margin-bottom: 30px;
        }
        .funcion-card {
            background: rgba(255, 255, 255, 0.95);
            border-radius: 15px;
            padding: 20px;
            box-shadow: 0 8px 25px rgba(0,0,0,0.15);
            transition: transform 0.3s ease;
        }
        .funcion-card:hover {
            transform: translateY(-5px);
        }
        .funcion-title {
            color: #2c3e50;
            font-size: 1.3em;
            font-weight: bold;
            margin: 0 0 10px 0;
        }
        .funcion-info {
            color: #7f8c8d;
            margin: 5px 0;
        }
        .funcion-info strong {
            color: #34495e;
        }
        .funcion-actions {
            margin-top: 15px;
            text-align: center;
        }
        .loading {
            text-align: center;
            color: white;
            font-size: 1.2em;
            padding: 40px;
        }
        .error {
            background: #e74c3c;
            color: white;
            padding: 15px;
            border-radius: 8px;
            text-align: center;
            margin: 20px 0;
        }
        .back-btn {
            position: absolute;
            top: 20px;
            left: 20px;
            background: rgba(255, 255, 255, 0.9);
            color: #2c3e50;
            padding: 10px 20px;
            border-radius: 8px;
            text-decoration: none;
            font-weight: bold;
        }
        .status-badge {
            display: inline-block;
            padding: 5px 12px;
            border-radius: 20px;
            font-size: 0.8em;
            font-weight: bold;
            text-transform: uppercase;
        }
        .status-activa {
            background: #d5f4e6;
            color: #27ae60;
        }
        .status-agotada {
            background: #fadbd8;
            color: #e74c3c;
        }
        .tipo-badge {
            display: inline-block;
            padding: 5px 12px;
            border-radius: 20px;
            font-size: 0.8em;
            font-weight: bold;
            text-transform: uppercase;
            margin-left: 10px;
        }
        .tipo-estreno {
            background: #fff3e0;
            color: #f57c00;
        }
        .tipo-normal {
            background: #e3f2fd;
            color: #1976d2;
        }
        .tipo-vip {
            background: #f3e5f5;
            color: #7b1fa2;
        }
        .tipo-matine {
            background: #e8f5e8;
            color: #2e7d32;
        }
    </style>
</head>
<body>
    <a href="/" class="back-btn">← Volver al Inicio</a>
    
    <div class="container">
        <div class="header">
            <h1>⏰ Funciones</h1>
            <p>Gestiona las funciones programadas del cine</p>
        </div>

        <div class="actions">
            <button class="btn btn-success" onclick="loadFunciones()">🔄 Cargar Funciones</button>
            <a href="/funciones/agregar" class="btn">➕ Agregar Función</a>
        </div>

        <div id="funcionesContainer">
            <div class="loading">
                <p>🔄 Cargando funciones...</p>
                <p>Haz clic en "Cargar Funciones" para ver el catálogo</p>
            </div>
        </div>
    </div>

    <script>
        const API_BASE = '/api/funciones';

        async function loadFunciones() {
            try {
                const response = await fetch(API_BASE);
                const data = await response.json();
                
                if (data.success) {
                    displayFunciones(data.data);
                } else {
                    showError('Error al cargar las funciones: ' + data.message);
                }
            } catch (error) {
                showError('Error de conexión: ' + error.message);
            }
        }

        function displayFunciones(funciones) {
            const container = document.getElementById('funcionesContainer');
            
            if (funciones.length === 0) {
                container.innerHTML = '<div class="error">No hay funciones disponibles</div>';
                return;
            }

            container.innerHTML = `
                <div class="funciones-grid">
                    ${funciones.map(funcion => {
                        const statusClass = funcion.estado === 'activa' ? 'status-activa' : 'status-agotada';
                        const tipoClass = `tipo-${funcion.tipo.toLowerCase()}`;
                        
                        return `
                        <div class="funcion-card">
                            <h3 class="funcion-title">${funcion.pelicula ? funcion.pelicula.titulo : 'Película no disponible'}</h3>
                            <div class="funcion-info"><strong>Sala:</strong> ${funcion.sala ? funcion.sala.numero : 'Sala no disponible'}</div>
                            <div class="funcion-info"><strong>Fecha:</strong> ${funcion.fecha}</div>
                            <div class="funcion-info"><strong>Hora:</strong> ${funcion.horaInicio} - ${funcion.horaFin}</div>
                            <div class="funcion-info"><strong>Precio:</strong> $${funcion.precio}</div>
                            <div class="funcion-info"><strong>Asientos:</strong> ${funcion.asientosDisponibles} disponibles de ${funcion.asientosDisponibles + funcion.asientosOcupados}</div>
                            <div class="funcion-info">
                                <strong>Estado:</strong> <span class="status-badge ${statusClass}">${funcion.estado}</span>
                                <span class="tipo-badge ${tipoClass}">${funcion.tipo}</span>
                            </div>
                            <div class="funcion-actions">
                                <a href="/funciones/${funcion.id}" class="btn">👁️ Ver Detalle</a>
                                <button class="btn" onclick="editFuncion(${funcion.id})">✏️ Editar</button>
                                <button class="btn btn-danger" onclick="deleteFuncion(${funcion.id})">🗑️ Eliminar</button>
                            </div>
                        </div>
                    `}).join('')}
                </div>
            `;
        }

        function showError(message) {
            const container = document.getElementById('funcionesContainer');
            container.innerHTML = `<div class="error">${message}</div>`;
        }

        async function deleteFuncion(id) {
            if (confirm('¿Estás seguro de que quieres eliminar esta función?')) {
                try {
                    const response = await fetch(`${API_BASE}/${id}`, {
                        method: 'DELETE'
                    });
                    const data = await response.json();
                    
                    if (data.success) {
                        alert('Función eliminada exitosamente');
                        loadFunciones(); // Recargar la lista
                    } else {
                        alert('Error al eliminar la función: ' + data.message);
                    }
                } catch (error) {
                    alert('Error al eliminar la función: ' + error.message);
                }
            }
        }

        function editFuncion(id) {
            alert('Funcionalidad de edición pendiente de implementar');
        }

        // Cargar funciones al cargar la página
        window.onload = function() {
            loadFunciones();
        };
    </script>
</body>
</html>

